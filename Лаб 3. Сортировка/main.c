#include "sortunit.h"
#include <stdio.h>
#include <stdlib.h>
#define N 10


int main()
{
    //int arr[N] = {1, 6, 3, 3, 5, 4, 5, 9, 8, 5};
    //bubble_sort(arr, N, sizeof(arr[0]), cmp);

    // проверка cmp
    // int a = 5;
    // int b = -5;
    // int res = cmp(&a, &b);
    // printf("res = %d\n", res);

    

    return 0;
}